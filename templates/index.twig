{% extends 'templates/_document.twig' %}

{% block content %}
  {# {{ renderComponent('GridPostsArchive', {posts: posts}) }} #}
  {# {% for component in post.meta('pageComponents') %}
    {{ renderComponent(component) }}
  {% endfor %} #}

  {# <div class=" bg-black text-4xl">
Raj
  </div> #}
<div class=" ">
  {{ renderComponent("HomePage")  }}

  </div>
{% endblock %}
